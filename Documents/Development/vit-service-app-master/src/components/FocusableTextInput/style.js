import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    borderColor: '#999',
    borderWidth: 0.5,
    borderRadius: 10,
  },
  inputActive: {
    borderColor: '#f00',
  }
});